export interface State {
  isCopied: boolean;
  userId: string;
  radius: string;
  headerColor: string;
  bodyColor: string;
  textColor: string;
  accentColor: string;
  dueDate: boolean;
  priority: boolean;
  hideCompleted: boolean;
}
